<h1 class="doc-page-title">Material theme</h1>

<p>
  Like with bootstrap, it's not a lot of code to create a material-design theme
  using the available SASS variables and a couple animations before importing
  the styles of the addon.
</p>

{{code-snippet name="material-theme-1.scss" class="max-500-height"}}

<h3>Regular selects without search</h3>
<div style="display: flex;">
  <div style="flex: 1; padding: 5px;">
    <PowerSelect
      @triggerClass="material-theme-trigger"
      @dropdownClass="material-theme-dropdown"
      @options={{countries}}
      @selected={{country}}
      @placeholder="Country"
      @searchField="name"
      @onChange={{action (mut country)}} as |country|>
      {{country.name}}
    </PowerSelect>
  </div>
  <div style="flex: 1; padding: 5px;">
    <PowerSelect
      @triggerClass="material-theme-trigger"
      @dropdownClass="material-theme-dropdown"
      @options={{cities}}
      @selected={{city}}
      @placeholder="City"
      @allowClear={{true}}
      @onChange={{action (mut city)}} as |city|>
      {{city}}
    </PowerSelect>
  </div>
</div>

<h3>Regular selects with search</h3>
<PowerSelect
  @triggerClass="material-theme-trigger"
  @dropdownClass="material-theme-dropdown"
  @options={{countries}}
  @selected={{country2}}
  @placeholder="Country"
  @searchField="name"
  @searchEnabled={{true}}
  @onChange={{action (mut country2)}} as |country|>
  {{country.name}}
</PowerSelect>

<h3>Multiple selects with search</h3>
<PowerSelectMultiple
  @triggerClass="material-theme-trigger"
  @dropdownClass="material-theme-dropdown"
  @options={{countries}}
  @selected={{selectedCountries}}
  @placeholder="Country"
  @searchField="name"
  @searchEnabled={{true}}
  @onChange={{action (mut selectedCountries)}} as |country|>
  {{country.name}}
</PowerSelectMultiple>
<h3>Groups</h3>
<PowerSelect
  @triggerClass="material-theme-trigger"
  @dropdownClass="material-theme-dropdown"
  @options={{groupedNumbers}}
  @selected={{number}}
  @placeholder="Choose a number"
  @onChange={{action (mut number)}} as |num|>
  {{num}}
</PowerSelect>

<h3>Select with clear btn</h3>
<PowerSelect
  @triggerClass="material-theme-trigger"
  @dropdownClass="material-theme-dropdown"
  @options={{countries}}
  @selected={{country3}}
  @placeholder="Country"
  @searchField="name"
  @allowClear={{true}}
  @searchEnabled={{true}}
  @onChange={{action (mut country3)}} as |country|>
  {{country.name}}
</PowerSelect>
<br>
<br>
<p>
  This theme doesn't attempt to provide totally accurate replica of the angular directive,
  but it's a good start.
</p>

<p>To use it in your app, SASS users should import the theme before importing the main stylesheet:</p>

{{code-snippet name="material-theme-2.scss"}}

<p>
  As with the bootstrap one, this skin comes pre-compiled in the app, apps that don't use
  SASS can include it from the <code>ember-cli-build.js</code> file.
</p>

{{code-snippet name="material-theme-1-js.js" class="max-500-height"}}

<p>In the next section we're going to explain how come this select can be animated without any javascript!</p>

<div class="doc-page-nav">
  {{#link-to "public-pages.cookbook.bootstrap-theme" class="doc-page-nav-link-prev"}}&lt; Bootstrap theme{{/link-to}}
  {{#link-to "public-pages.cookbook.css-animations" class="doc-page-nav-link-next"}}CSS animations &gt;{{/link-to}}
</div>