<p>Countries:</p>
<ul>
  {{#unless extra.field}}
    {{#each options as |option index|}}
      <li>{{index}}. {{option.name}}</li>
    {{/each}}
  {{else}}
    {{#each options as |option index|}}
      <li>{{index}}. {{get option extra.field}}</li>
    {{/each}}
  {{/unless}}
</ul>
