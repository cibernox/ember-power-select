<PowerSelectMultiple
  @selected={{this.selectedCities}}
  @options={{this.cities}}
  @onChange={{action (mut this.selectedCities)}}
  @onInput={{this.checkLength}} as |name|>
  {{name}}
</PowerSelectMultiple>