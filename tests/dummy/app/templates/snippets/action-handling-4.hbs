<PowerSelectMultiple
  @selected={{this.selectedCities}}
  @options={{this.cities}}
  @onChange={{fn (mut this.selectedCities)}}
  @onInput={{this.checkLength}} as |name|>
  {{name}}
</PowerSelectMultiple>