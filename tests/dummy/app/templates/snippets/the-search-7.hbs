<PowerSelect
  @searchEnabled={{true}}
  @options={{diacritics}}
  @searchPlaceholder="Type to filter..."
  @selected={{person}}
  @onChange={{action (mut person)}} as |name|>
  {{name}}
</PowerSelect>