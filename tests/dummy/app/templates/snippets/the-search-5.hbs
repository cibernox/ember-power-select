<PowerSelect
  @searchEnabled={{true}}
  @options={{people}}
  @matcher={{myMatcher}}
  @selected={{selectedPerson}}
  @onChange={{action (mut selectedPerson)}} as |person|>
  {{person.name}} {{person.surname}}
</PowerSelect>