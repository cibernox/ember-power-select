<PowerSelect @options={{countries}} @selected={{destination}} @onChange={{action "foo"}} as |country|>
  <img src={{country.flagUrl}} class="icon-flag" alt="Flag of {{country.name}}">
  <strong>{{country.name}}</strong>
</PowerSelect>