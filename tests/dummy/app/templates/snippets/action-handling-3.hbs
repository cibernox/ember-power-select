<PowerSelectMultiple
  @selected={{selectedCities}}
  @options={{cities}}
  @onChange={{action (mut selectedCities)}}
  @onKeydown={{action "handleKeydown"}} as |name|>
  {{name}}
</PowerSelectMultiple>