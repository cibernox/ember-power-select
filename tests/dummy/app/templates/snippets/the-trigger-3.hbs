<PowerSelect
  @searchEnabled={{true}}
  @options={{countries}}
  @placeholder="Please, select one destination"
  @selected={{selected}}
  @searchField="name"
  @onChange={{action (mut selected)}} as |country|>
  {{country.name}}
</PowerSelect>