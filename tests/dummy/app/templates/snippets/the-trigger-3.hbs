{{#power-select
  options=countries
  placeholder="Please, select one destination"
  selected=selected
  searchField="name"
  onchange=(action (mut selected))
  as |country|
}}
  {{country.name}}
{{/power-select}}