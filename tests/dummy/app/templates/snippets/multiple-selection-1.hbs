<PowerSelectMultiple
  @searchEnabled={{true}}
  @options={{names}}
  @selected={{name}}
  @placeholder="Select some names..."
  @onChange={{action (mut name)}} as |name|>
  {{name}}
</PowerSelectMultiple>