{{#power-select
  options=names
  renderInPlace=true
  selected=name
  onChange=(action (mut name))
  as |name|
}}
  {{name}}
{{/power-select}}
(Looks the same but inspect the DOM to see the difference)
