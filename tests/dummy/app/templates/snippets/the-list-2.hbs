<PowerSelect
  @options={{names}}
  @renderInPlace={{true}}
  @selected={{name}}
  @onChange={{action (mut name)}} as |name|>
  {{name}}
</PowerSelect>
(Looks the same but inspect the DOM to see the difference)
