<PowerSelectMultiple
  @options={{options}}
  @selected={{selected}}
  @onChange={{action (mut selected)}}
  @searchEnabled={{true}}
  @onKeydown={{action "createOnEnter"}} as |number|>
  {{number}}
</PowerSelectMultiple>
