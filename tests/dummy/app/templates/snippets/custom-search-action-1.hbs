{{#power-select
  search=(action "searchRepo")
  selected=selected
  onChange=(action (mut selected))
  as |repo|
}}
  {{repo.full_name}}
{{/power-select}}
(It might fail if the API limit is exceeded)