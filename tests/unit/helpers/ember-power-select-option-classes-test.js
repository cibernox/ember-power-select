import { emberPowerSelectOptionClasses } from '../../../helpers/ember-power-select-option-classes';
import { module, test } from 'qunit';

module('Unit | Helper | ember power select option classes');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = emberPowerSelectOptionClasses(42);
  assert.ok(result);
});
