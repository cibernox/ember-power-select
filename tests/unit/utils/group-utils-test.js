import groupUtils from '../../../utils/group-utils';
import { module, test } from 'qunit';

module('Unit | Utility | group utils');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = groupUtils();
  assert.ok(result);
});
