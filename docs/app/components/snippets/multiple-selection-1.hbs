<PowerSelectMultiple
  @searchEnabled={{true}}
  @options={{this.names}}
  @selected={{this.name}}
  @labelText="Name"
  @placeholder="Select some names..."
  @onChange={{fn (mut this.name)}}
  as |name|
>
  {{name}}
</PowerSelectMultiple>