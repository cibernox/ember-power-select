<PowerSelect
  @options={{this.names}}
  @selected={{this.aName}}
  @placeholderComponent={{component "weird-placeholder"}}
  @labelText="Name"
  @onChange={{fn (mut this.aName)}} as |name|>
  {{name}}
</PowerSelect>