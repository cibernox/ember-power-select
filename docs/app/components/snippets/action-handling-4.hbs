<PowerSelect
  @multiple={{true}}
  @selected={{this.selectedCities}}
  @options={{this.cities}}
  @onChange={{fn (mut this.selectedCities)}}
  @labelText="Country"
  @onInput={{this.checkLength}}
  as |name|
>
  {{name}}
</PowerSelect>