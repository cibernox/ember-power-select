<PowerSelect
  @options={{this.names}}
  @renderInPlace={{true}}
  @selected={{this.name}}
  @onChange={{fn (mut this.name)}} as |name|>
  {{name}}
</PowerSelect>
(Looks the same but inspect the DOM to see the difference)
