<PowerSelectMultiple
  @selected={{this.selectedCities}}
  @options={{this.cities}}
  @onChange={{fn (mut this.selectedCities)}}
  @labelText="Country"
  @onKeydown={{this.handleKeydown}} as |name|>
  {{name}}
</PowerSelectMultiple>