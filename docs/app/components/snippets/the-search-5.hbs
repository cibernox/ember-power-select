<PowerSelect
  @searchEnabled={{true}}
  @options={{this.people}}
  @matcher={{this.myMatcher}}
  @selected={{this.selectedPerson}}
  @labelText="Person"
  @onChange={{fn (mut this.selectedPerson)}}
  as |person|
>
  {{person.name}}
  {{person.surname}}
</PowerSelect>