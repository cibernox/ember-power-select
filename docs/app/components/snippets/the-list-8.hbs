<PowerSelect
  @searchEnabled={{false}}
  @options={{this.countries}}
  @selected={{this.country}}
  @searchField="name"
  @labelText="Country"
  @onChange={{fn (mut this.country)}}
  as |country|>
  {{country.name}}
</PowerSelect>