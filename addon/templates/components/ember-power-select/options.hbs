{{#each options as |opt|}}
  {{#if opt.groupName}}
    <li class="ember-power-select-group">
      <span class="ember-power-select-group-name">{{opt.groupName}}</span>
      <ul class="ember-power-select-options nested">
        {{#ember-power-select/options highlighted=highlighted selection=selection options=opt.options highlight=(action highlight) select=(action select) dropdown=dropdown as |option|}}
          {{yield option}}
        {{/ember-power-select/options}}
      </ul>
    </li>
  {{else}}
    <li class="ember-power-select-option {{ember-power-select-option-classes opt selection highlighted}}" onclick={{action select opt dropdown.close}} onmouseover={{action highlight opt}}>
      {{yield opt}}
    </li>
  {{/if}}
{{/each}}
