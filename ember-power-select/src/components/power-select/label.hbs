{{#let (element (or @labelTag "label")) as |LabelTag|}}
  <LabelTag
    id={{@labelId}}
    class="ember-power-select-label"
    ...attributes
    for={{@triggerId}}
    {{on "click" this.onLabelClick}}
  >
    {{@labelText}}
  </LabelTag>
{{/let}}